#!/usr/bin/python3
def remove_char_at(str, n):
    count = -1
    temp = ""
    for i in str:
        count += 1
        if count == n:
            continue
        else:
            temp += i
    return temp
